---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: datavolumes
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: {{ .Values.datavolumes.git.repoUrl }}
    targetRevision: {{ .Values.datavolumes.git.targetRevision }}
    path: {{ .Values.datavolumes.git.path }}
    helm:
      values: |
        datavolumes:
          imageUrl: {{ .Values.datavolumes.imageUrl }}
          volumeSnapshotClassName: {{ .Values.datavolumes.volumeSnapshotClassName }}
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated: {}
